分割实例检测
==============================

在 **分割实例** 检测中，模型会定位图像中的对象并生成精准的定位边界。在现实应用中， **分割实例** 常用作识别不规则外形的物体。

模型选择情景
------------------------------

**分割实例** 可以用以检测图像中一个或多个不同物体的数量及位置。

在 **分割实例** 中，可以通过只建立一个物体标签来进行单一种类物品的识别，如：在大量混合物体中寻找并分离处某一特定种类的物体。也可以同时建立并学习多种物体的标签，从而达到将多种物体分别识别的效果。
